# 🚀 Инструкция по настройке Poker Assistant Bot

Эта инструкция поможет тебе запустить бота с нуля.

---

## 📋 Шаг 1: Создание Telegram-бота

### 1.1 Открой Telegram и найди @BotFather

@BotFather — это официальный бот для создания других ботов.

### 1.2 Создай нового бота

Отправь команду:
```
/newbot
```

### 1.3 Придумай имя бота

Например:
```
Poker Assistant
```

### 1.4 Придумай username бота

Username должен заканчиваться на `bot`. Например:
```
poker_assistant_pro_bot
```

### 1.5 Скопируй токен

BotFather отправит тебе токен вида:
```
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

**⚠️ ВАЖНО:** Этот токен никому не показывай! Это как пароль от бота.

---

## 📋 Шаг 2: Клонирование репозитория

### 2.1 Открой терминал (командную строку)

**Windows:** Win+R → `cmd` → Enter
**Mac/Linux:** Открой Terminal

### 2.2 Клонируй репозиторий

```bash
git clone https://github.com/remontbot/poker-assistant-bot.git
cd poker-assistant-bot
```

Если у тебя нет Git:
- **Windows:** Скачай с https://git-scm.com/download/win
- **Mac:** `brew install git`
- **Linux:** `sudo apt-get install git`

---

## 📋 Шаг 3: Установка Python

### 3.1 Проверь версию Python

```bash
python --version
```

Должна быть версия **3.10 или выше**.

Если Python не установлен:
- **Windows:** Скачай с https://www.python.org/downloads/
- **Mac:** `brew install python3`
- **Linux:** `sudo apt-get install python3`

---

## 📋 Шаг 4: Создание виртуального окружения

### 4.1 Создай виртуальное окружение

```bash
python -m venv venv
```

### 4.2 Активируй виртуальное окружение

**Windows:**
```bash
venv\Scripts\activate
```

**Mac/Linux:**
```bash
source venv/bin/activate
```

После активации в начале строки появится `(venv)`.

---

## 📋 Шаг 5: Установка зависимостей

```bash
pip install -r requirements.txt
```

Это установит все необходимые библиотеки.

---

## 📋 Шаг 6: Настройка переменных окружения

### 6.1 Создай файл `.env`

Скопируй `.env.example` в `.env`:

**Windows:**
```bash
copy .env.example .env
```

**Mac/Linux:**
```bash
cp .env.example .env
```

### 6.2 Открой `.env` в текстовом редакторе

Например, в Notepad++ (Windows) или nano (Linux):

```bash
notepad .env
```

или

```bash
nano .env
```

### 6.3 Вставь токен бота

Замени `your_bot_token_here` на токен, который дал тебе BotFather:

```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
DEBUG=True
DATABASE_PATH=data/poker.db
```

Сохрани файл.

---

## 📋 Шаг 7: Запуск бота

### 7.1 Запусти бота

```bash
python bot.py
```

Если всё настроено правильно, увидишь:
```
Bot started successfully!
Press Ctrl+C to stop.
```

### 7.2 Открой Telegram и найди своего бота

Найди бота по username (например, `@poker_assistant_pro_bot`).

### 7.3 Отправь команду `/start`

Бот должен ответить!

---

## 🐛 Решение проблем

### Проблема: "ModuleNotFoundError: No module named 'telegram'"

**Решение:** Установи зависимости:
```bash
pip install -r requirements.txt
```

### Проблема: "Unauthorized: bot token is invalid"

**Решение:** Проверь токен в `.env` файле. Скопируй его заново от BotFather.

### Проблема: "sqlite3.OperationalError: unable to open database file"

**Решение:** Создай папку `data`:
```bash
mkdir data
```

### Проблема: Бот не отвечает

**Решение:**
1. Проверь, что бот запущен (`python bot.py`)
2. Проверь, что токен правильный
3. Проверь интернет-соединение

---

## 📱 Следующие шаги

После успешного запуска бота:

1. **Протестируй базовый функционал:**
   - `/start` — приветствие
   - `/new_hand` — начать новую раздачу
   - `/help` — справка

2. **Изучи код:**
   - `bot.py` — главная логика
   - `poker_logic/` — расчёты эквити и GTO
   - `ai_analysis/` — анализ игроков

3. **Добавь новые функции:**
   - Смотри план разработки в README.md

---

## 🆘 Нужна помощь?

Если что-то не работает:
1. Проверь эту инструкцию ещё раз
2. Посмотри логи ошибок в консоли
3. Создай Issue на GitHub: https://github.com/remontbot/poker-assistant-bot/issues

Удачи! 🃏
